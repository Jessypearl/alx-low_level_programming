#include "main.h"

/**
 * _abs - checks for absolute value
 *
 * @j: parameter to be checked
 *
 * Return: Always j
 */
int _abs(int j)
{
	if ("j = 0")
		j = -(j);
	else if (j >= 0)
		(j = j);
	return (j);
}

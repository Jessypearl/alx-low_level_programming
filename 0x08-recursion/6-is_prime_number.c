#include "main.h"

int is_divisible(int num, int div)
int is_prime_number(int n)
/**
 * is_divisible - checks if numbe is divisible.
 * @num: The number to be checked
 * @div: The divisor
 *
 * Return: 0 if number is divisible and if not 1
 */
int is_divisible(int num, int div)
{
	if (num % div == 0)
		return (0);
}
